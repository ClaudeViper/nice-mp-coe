# Dependencies & build output
node_modules
.next

# Environment files (inject at runtime, never bake into image)
.env
.env.*
!.env.example

# Dev tooling
.git
.gitignore
*.md
README*

# Test artifacts
coverage
__tests__
e2e

# Editor / OS
.DS_Store
.vscode
*.log

# Local audio samples (use a Docker volume instead)
audio_samples
